# Claude Code Instructions — Codex Brief Generator

Use the root `SKILL.md` as the canonical instructions for this repository.

For Claude Code skill installation, copy the packaged skill directory:

```text
skills/autonomous-ai-agents/codex-brief-generator/
```

to:

```text
~/.claude/skills/autonomous-ai-agents/codex-brief-generator/
```

The directory name and the `name:` field are both `codex-brief-generator`. Do not reference older repository layouts.
